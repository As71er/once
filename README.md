# Once

A REST API focused on working as a local music archive, so a client in a LAN can upload songs to a respective release and it'll populate most of its data from the tags found in the audio files. The fields are based on some standard ID3 tags and naming conventions from [MusicBrainz](https://picard-docs.musicbrainz.org/en/latest/variables/tags_basic.html).
